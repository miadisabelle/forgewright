# PDE Review — Human + Companion Notes

**Reviewer**: Mia 🧠 (PDE Review Companion)
**Date**: 2026-03-16
**PDE ID**: 8e8a9e9f-9fc1-4c16-b387-e4bb22444433

---

## Corrections from Human Annotations

### Medicine Wheel: 15 packages, not 7
Path: `/workspace/repos/jgwill/medicine-wheel/src` — scan all subdirectories.
All packages are published on npm. Verify with `npm view <package-name>`.

### Existing Code to Borrow (SOUTH deep-search targets)

| Path | What's There |
|------|-------------|
| `repos/jgwill/src/coaia-pde` | Existing PDE implementation code |
| `repos/jgwill/src/coaia-planning` | Planning layer code |
| `repos/jgwill/coaia-visualizer` | STC visualization (borrow for spec 05) |
| `repos/jgwill/smcraft` | MCP + WebUI + 2 runtimes (Python + TypeScript). Real code, not just specs. May need enhancement — assign agents to extend if needed. |
| `repos/miadisabelle/mia-qmd` | Internal embedding + search system. Use instead of node-llama-cpp for MVP. |

### smcraft Is Real
Has: MCP server, Web UI, Python runtime, TypeScript runtime.
Still new — may need development. Consider assigning sub-agents to extend it.

### Medicine Wheel Submodule
May be an unmerged/unpulled submodule. Check status first:
```bash
git submodule status
# or check if repos/jgwill/medicine-wheel/src has content
ls repos/jgwill/medicine-wheel/src/
```
Link packages locally if not available from npm.

### Agent Iteration Model
"30+ agents" means **multiple iterations** — not one-shot. Pattern:
1. Deep-search agents research
2. Implementation agents build
3. Review agents inspect + correct
4. Refactoring agents clean up
5. Test agents validate
Quality comes from iteration, not parallelism alone.

### Chronicle Side-Effect
At each major milestone, spawn `/tushell-session-chronicle` sub-agent to draft a chapter. Goal: produce a near-book-length chronicle by session end.

---

## Missing Action Stack Items

Add these to the execution plan:

```
- [ ] Deep-search repos/jgwill/src for coaia-pde, coaia-planning, coaia-visualizer code
- [ ] Check medicine-wheel submodule status, pull/link packages locally
- [ ] Deep-search repos/miadisabelle/mia-qmd for embedding + search patterns
- [ ] Integrate QMD search into forgewright (replaces node-llama-cpp for spec search)
- [ ] Spawn tushell-session-chronicle sub-agent at each major milestone
```

## Resolved Ambiguities

- ✅ Medicine-wheel packages: all published on npm
- ✅ smcraft: has real implementation (MCP + WebUI + 2 runtimes)
- ✅ node-llama-cpp: use mia-qmd instead for MVP
- ✅ 30+ agents: iterative quality model, not one-shot
